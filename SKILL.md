---
name: paper-workflow
version: 0.3.1
description: >
  8 阶段论文全流程编排器，覆盖从选题到投稿的完整实证研究生命周期。
  触发条件：用户说"写论文""开始写论文""论文工作流""继续论文""论文写到哪了"
  "选题""研究设计""实证分析""写初稿""打磨论文"以及任何涉及完整论文流程的请求。
  通过 Obsidian 仪表盘实现跨会话状态持久化，支持阶段跳转和迭代回退。
---

# Paper Workflow — 8 阶段论文编排器

## 可用工具

**实证问题排查**：见 §5.1 实证问题溯源。遇到平行趋势/不显著等实证问题时，先加载 `research-discovery` 技能分层诊断（数据层/方法层/理论层），需要外部实操方案时再加载 `research-media-skill` 搜经管之家。

论文流程中涉及 Python 数据处理时，使用独立 conda 环境 `research`：

```bash
# 方式 1：直接调 pyresearch（推荐，已隔离 PYTHONPATH）
pyresearch script.py
pyresearch -c "print('hello')"

# 方式 2：终端工具
terminal("pyresearch -c \"import pandas as pd; ...\"")
```

环境位置：`/opt/homebrew/Caskroom/miniforge/base/envs/research/`
包：pandas 3.0.3, numpy 2.4.6, scipy, matplotlib, openpyxl, requests, jupyter

> ⚠️ 此环境与 Hermes 的 Python 3.11 venv 隔离。通过 `pyresearch` wrapper 调用时会自动清空 PYTHONPATH，避免 3.11 的 numpy .so 污染。

## 核心设计

本 skill 是论文写作的**总调度器**，不替代任何现有工具，而是：
- 读取 Obsidian 项目仪表盘 → 知道当前状态和上下文
- 调用现有 skill（meng-skills、aers-index、academic-literature-collector、obsidian 等）
- 在阶段边界写入决策门
- 每次会话结束后更新仪表盘

### 项目结构

```
研究/<项目名>/                  ← 直接在 Obsidian vault 根目录下（2026-07 由"论文/"更名）
├── 研究发现/                  ← research-discovery 技能空间（实证发现管理）
│   ├── 00-发现索引.md         ← 看板：发现清单 + 状态
│   ├── 01-发现/F-xxx.md       ← 每条发现一个文件（触发→诊断→决策→沉淀）
│   ├── 02-假设修正.md         ← 聚合：假设修正史
│   ├── 03-选题孵化.md         ← 聚合：孵化中的新选题
│   └── 04-方法论沉淀.md       ← 聚合：跨论文可复用经验
└── 论文写作/                  ← 本 skill 空间（原 论文/<项目名>/ 结构整体平移）
    ├── 00-项目仪表盘.md          ← 跨会话状态中枢
    ├── 01-选题/
    │   └── 研究计划.md           ← 研究设计权威文档，随实证更新同步修订
    ├── 02-文献/
    │   ├── 文献综述.md
    │   └── 论文精读/
    ├── 03-理论/
    │   ├── 制度背景.md
    │   └── 理论推演.md           ← 含研究假设（需随实证结果更新）
    ├── 04-数据/
    │   ├── 数据需求清单.md
    │   ├── 数据构造/
    │   └── 分析命令/              ← do 文件 + log + CSV
    ├── 05-实证/
    │   └── 主回归结果.md          ← 全量表（详细系数，非摘要）
    ├── 06-论文稿/
    └── 07-投稿/
```

两个目录通过 wiki-link 双向索引：研究发现文件「触发」段链接论文写作来源文件，「决策与影响」段链接被修改的论文文件；论文写作修改处追加反向标注 `> ⚠️ 依据 [[F-001-标题关键词]] 修正（日期）`。**发现文件命名 `F-序号-标题关键词.md`（序号项目内递增），反向链接必须写完整文件名**——`[[F-001]]` 解析不到 `F-001-标题关键词.md`，会断链。

**关键文件索引（改实证必查）**：

| 改了 do 文件的什么 | 必须同步更新哪些 Obsidian 文件 |
|------|------|
| 新增/删除回归表 | 主回归结果.md、README.md、仪表盘 |
| DV 构造或版本变化 | 研究计划.md（§2.1）、理论推演.md（假设部分）、数据构造/ |
| 处理变量变化 | 研究计划.md（§2.2）、理论推演.md（假设部分）、制度背景.md |
| 经济后果结果变化 | 研究计划.md（§6）、仪表盘 |
| 机制检验结果变化 | 理论推演.md（H2/H3）、仪表盘 |
| 假设方向/数量调整 | 理论推演.md（§四）、研究计划.md（§1.3） |
| 实证结果意外/假设修正 | 研究发现/（F-xxx 文件 + 02-假设修正.md），理论推演.md 修改处追加反向标注 |

## 交互规范（强制执行）

本 skill 下的所有用户交互必须遵守以下两条规则：

### 规则 1：提问用弹窗，不用文字列表

向用户提问时，**必须使用 `clarify` 工具**，不得在回复正文中罗列问题。

```
✅ clarify(question="主回归用哪个版本？", choices=["V1 标准口径", "V2 严格口径", "V1+V2 并列"])
❌ "你想用 V1 还是 V2？还是两个都要？"
```

`clarify` 自动提供可点击选项 + 「Other」自由输入。每次一个问题，逐个确认。

### 规则 2：执行前消歧——先问再做

以下情况**必须先用 `clarify` 提问，等确认后再执行**：

| 触发条件 | 示例 |
|---------|------|
| 指令有多种合理解读 | "把那个回归跑一下"——哪个 DV/样本？ |
| 存在替代方案且有实质差异 | 分组检验 vs 连续变量回归 |
| 操作会改变论文结构 | 删除某张表、移动某节 |
| 结果可能被用户否决 | 跑大概率不显著的回归 |
| 涉及文件覆盖或删除 | 重写 Obsidian 文档、删除旧 CSV |

**不需要提问**：简单数据查看、参数已明确的回归、纯机械操作。

## 会话启动协议（强制执行）

**每个新会话的第一步**，读取项目仪表盘和跨 Agent 记忆。

### 步骤 1：项目仪表盘

> ⚠️ **iCloud 同步延迟**：Obsidian vault 在 iCloud 上。程序化创建的文件夹可能不会立即出现在 Obsidian 侧边栏。如果用户说"没看到文件夹"，先用 `ls` 验证磁盘上确实存在，告知用户等待同步或手动在 Finder 中访问该文件夹触发同步。
```text
read_file("研究/<项目名>/论文写作/00-项目仪表盘.md")
```

从仪表盘提取：
- 当前阶段和状态
- 已完成工作和产出
- 本轮待完成任务
- 上次会话的决策和待办
- 关键文件路径

### 步骤 2：跨 Agent 记忆（如存在）

检查 Obsidian vault 根目录下 `agent memory/` 文件夹：
- `agent memory/学术/论文-<项目关键词>/` → 读取，获取论文状态、关键结果、待办
- `agent memory/学术/_方法/` → 仅在遇到 Stata/数据问题时读取
- `agent memory/学术/_设计/` → 仅在选题/研究设计阶段读取

**路由规则**：只读文件夹名匹配当前任务的目录，不读全量。不确定时不读，等任务明确后再加载。发现类经验沉淀统一走 `agent memory/学术/_方法/`（与 research-discovery 一致）。

然后直接进入对应阶段，**不要重新确认用户意图**（仪表盘就是意图记录）。

## 8 阶段流程

```
阶段 0：选题设计
阶段 1：文献检索与综述
阶段 2：理论分析与研究假设
     ═══════ 快速迭代环入口 ═══════
阶段 3：研究设计
阶段 4：数据构建
阶段 5：实证分析
     ├── 结果判断（决策门）
     └── 未通过→ 回退策略
     ═══════ 快速迭代环出口 ═══════
阶段 6：论文写作
阶段 7：打磨与投稿
```

---

## 阶段 0：选题设计

**定位**：结构化对话，不自动化。AI 的角色是引导和检查，决策权在用户。

### 0.1 文献全景扫描

- 检查 Zotero 精读库覆盖范围
- 加载 aers-index，查找 AERS 中与该方向的 novelty-check 方法论
- 产出：领域地图（谁做了什么、用什么方法、DV 是什么、剩余空白在哪）

### 0.2 空白定位对话

**第一步：加载护栏**。读取 `references/edmans-guardrails.md`，获取完整 7 条 Edmans 护栏清单。

逐条讨论可能的空白方向，用护栏过滤。

产出：选题短名单（3-5 个方向），存入 `01-选题/选题短名单.md`

**淘汰方向的处理**：判断为不可行的方向，记录到 `01-选题/淘汰选题记录.md`（方向简述 + 淘汰原因 + 日期）。避免跨会话重复讨论已淘汰的选题。典型案例：政务服务数字化→企业 TFP（2025 年选题讨论中因已有大量文献覆盖而淘汰）。

### 0.3 选题确认

对每个候选方向：
1. **查新**：加载 aers-index → 调用 AERS novelty-check 方法论
2. **数据可行性**：CSMAR/Wind/手工收集是否可得
3. **识别可行性**：处理变量变异来源、处理组/控制组比例
4. **设定预期发表层次**：CSSCI 目标，务实不拔高

产出：最终选题 + 研究计划，存入 `01-选题/研究计划.md`

---

## 阶段 1：文献检索与综述

**定位**：串联现有工具，系统化产出。

### 流程

中文文献通过 `chinese-literature` 技能（三通道策略）完成：

1. **中文文献（被动）**：加载 `chinese-literature` → 运行 CNKI RSS 采集（`scripts/cnki-rss.py`）→ 获取 13 本顶刊最新论文
2. **中文文献（主动·CNKI）**：加载 `chinese-literature` → 按用户需求运行 CNKI 浏览器搜索（JS 注入）→ 含 CSSCI/EI/北大核心高级检索过滤、期刊收录查询、论文详情提取
3. **中文文献（主动·NCPSSD）**：加载 `chinese-literature` → 运行 NCPSSD API 搜索（`scripts/ncpssd-search.py`）→ 开放获取文献，零认证零验证码，全学科覆盖
4. **英文文献**：`academic-literature-collector` 当期 + OpenAlex 追查
5. **精读**：按 AERS 的 literature-review 方法论（PRISMA 2020）筛选
6. **综述撰写**：按主题组织（非作者名单堆砌），存入 `02-文献/文献综述.md`

检查点：
- [ ] 中文覆盖知网核心期刊近 5 年（RSS 监控 + 主动搜索双轨）
- [ ] CSSCI 期刊来源过滤已使用（高级检索 `#CSSCI` 复选框）
- [ ] 英文覆盖对应英文文献
- [ ] 每篇精读有笔记（Obsidian 双向链接）
- [ ] 综述明确了研究空白（为阶段 2 理论分析铺路）

---

## 阶段 2：理论分析与研究假设

**定位**：质量检查清单为主（A 主线），完整框架生成为可选（B 可选）。
AI 的角色：**展示可能理论路线，由用户选择和完善**。

本阶段详细模板见 `references/theoretical-analysis-template.md`。核心流程：

### 2.1 制度背景

检查清单：
- [ ] 处理变量的制度来源（政策文件/法规/行政命令）
- [ ] 推行时点和地区差异描述
- [ ] 制度细节足够支撑识别策略（为什么某些地区先推行？）

产出：`03-理论/制度背景.md`

### 2.2 理论推演

AI 引导用户探索可能的理论路线：

**第一步：列出候选理论透镜**
从 `references/theoretical-analysis-template.md` 的候选理论透镜速查表中提取至少 3 个，让用户选择。

**第二步：用户选定后，推演逻辑链**
对每个假设，明确：
- X → M1 → M2 → Y 的每步逻辑
- 每步的文献支撑（至少一篇引用）
- 方向预测（正向/负向/非线性）

**第三步：竞争性解释**
- 反向因果是否可能？（Y 变化→X 推行？）
- 遗漏变量？（有其他同时期政策？）
- 替代解释？（不经过你提出的机制，也能解释 Y 变化？）

产出：`03-理论/理论推演.md`，含：
- 选定的理论框架 + 排除的理由（为什么不选另外几个）
- 机制图（X → M → Y）
- H1/H2/H3 的完整表述

### 2.4 假设提出

每一条假设必须满足：
- [ ] 有理论依据（非"已有研究"的模糊引用）
- [ ] 有明确方向预测
- [ ] 可证伪（什么实证结果算"不支持假设"？）
- [ ] 不与已有文献的已知结论冲突（或说明了为什么冲突）

---

## 阶段 3-5：快速迭代环

这三个阶段形成一个闭环。阶段 5 的结果可能触发射向阶段 3/4/2/0 的回退。**意外结果的处理流程（分层诊断→差距分析→决策路由→沉淀）见 `research-discovery` 技能，发现文件归档于 `研究发现/01-发现/`。**

### 决策门规则

阶段 5 完成后，**逐组件评估**，不是只看主回归 p 值。主回归 p<0.01 但机制/平行趋势/经济后果部分失败是常见情况。

```
══════════ 逐组件决策门 ═══════════

A. 主回归 (H1)
   ✅ DV 全部显著 (p<0.05) 且方向正确
   ⚠️ 部分显著 → 考虑 DV 构造调整 → 回阶段 4
   ❌ 全部不显著 → 回阶段 2 或 0

B. 平行趋势 (事件研究法)
   ✅ 处理前系数不显著异于零（全样本）
   ⚠️ 某些子样本不过 → 如实记录，不是放弃的理由
      - 子样本样本量 < 2000 → 可接受「结构性不过」
      - 符号一致子样本可做辅助证据
   ❌ 全样本不过 → 加载 research-discovery 分层诊断 → 需外部实操方案时加载 research-media-skill 搜经管之家 → 回阶段 3（调整识别策略）

C. 机制检验
   ✅ 三步法全部通过 或 交互项显著
   ⚠️ Step 2 失败 → 检查 M 是否为固定特征
      - 若是（如 pc_any/SOE）→ 三步法不可用，改用交互项
      - 若不是 → 检查 M 构造是否有误
   ❌ 所有机制方案均失败 → 论文无机制可报告 → 回阶段 2

D. 稳健性
   ✅ 至少 2 项稳健性检验通过且系数稳定
   ⚠️ 仅 1 项通过 → 加做 1-2 项
   ❌ 全部不显著 → 主效应不稳健 → 回阶段 4

E. 经济后果
   ✅ 至少 1 项显著
   ⚠️ 全部不显著 → 论文经济含义段单薄，考虑替换变量
      - 常见调整：TobinQ→ROA、专利总量→专利知识宽度
   ❌ 不强制要求（经济后果非论文核心贡献）

═══════════════════════════════════════

综合判定：
✅ 进入阶段 6：A+B ✅ 且 C+D 至少一项通过
🔄 回退：A 或 B 未通过 → 回阶段 3-4
🔧 修补：C/D/E 部分失败但 A+B 通过 → 继续但需在论文中如实说明
```

**关键原则**：不要将「阶段 5 通过」简化为一个勾。每次会话开始时读取仪表盘时，必须验证仪表盘标记的状态是否与上述逐组件标准一致。仪表盘可能过度乐观（如标记「阶段 5 ✅」但机制检验从未跑过）。发现不一致 → 立即更新仪表盘状态标记。

### 阶段 5 → 阶段 2 回写（强制执行）

**每次阶段 5 产出任何变更后，必须回溯更新阶段 2（理论推演）和阶段 0（研究计划），不等待阶段 6。**

触发条件（以下任一发生时立即回写）：

| 触发事件 | 必须回写的文件 | 回写内容 |
|---------|--------------|---------|
| 处理变量定义变化（如新增 treat_score1、删除 d_zhengting） | 理论推演.md（假设部分）、研究计划.md（§2.2） | 新增/删除假设，更新理论依据 |
| 机制检验结果变化（通过/失败/废弃） | 理论推演.md（H2/H3）、研究计划.md（§1.3） | 标记通过✅/暂缺⚠️/废弃❌，附日期 |
| DV 构造或版本调整 | 研究计划.md（§2.1）、数据构造文档 | 更新公式和变量名 |
| 假设方向/数量变化 | 理论推演.md（§四）、研究计划.md（§1.3） | 完整重写假设段落 |
| 经济后果仅剩一项或全部不显著 | 研究计划.md（§6） | 如实更新，不过度解释 |
| 实证发现改变假设理解 | 研究发现/01-发现/F-xxx.md、理论推演.md | 创建发现文件（触发→诊断→决策→沉淀），假设处追加反向标注 |

**回写后检查**：
- [ ] 仪表盘「进度追踪」中 02 理论分析与假设的状态是否需要从 ✅ 改为 ⚠️？
- [ ] 仪表盘「关键决策记录」是否追加了新决策？
- [ ] 仪表盘「迭代记录」是否追加了本次回写？
- [ ] 研究计划版本号和日期是否更新？
- [ ] 若假设被实证修正：`研究发现/01-发现/` 是否已有 F-xxx 文件？理论推演.md 对应假设处是否追加了 `> ⚠️ 依据 [[F-001-标题关键词]] 修正（日期）` 反向标注（完整文件名）？

### 迭代记录格式

在仪表盘 `迭代记录` 中，每轮回退记录：
```markdown
### 迭代 N（日期 → 日期）
- **回退原因**: 哪条假设被推翻 / 什么调整失败
- **尝试的调整**: ①…②…③…
- **最终决策**: 继续 / 回退 / 放弃
- **意外发现**: （如有）新的显著关系或方向相反的结果 → 创建 `研究发现/01-发现/F-序号-标题关键词.md` 并在本文档用完整文件名链接该文件
- **经验**: 避免下次犯同样错误
```

### 阶段 3：研究设计

**定位**：识别策略审计 + 变量构造方案。

从 AERS 加载方法论补充（通过 aers-index）：
- [ ] DID：平行趋势检验、bacondecomp、honestdid、csdid（staggered 时）— **具体 Stata 命令和诊断方法见 `stata-regression` 技能**
- IV：弱 IV 检验、过度识别、排除限制论证
- 其他方法按需加载

检查清单：
- [ ] 处理变量定义清晰（时点、处理组、强度）
- [ ] 平行趋势假设有支持（事件研究图 + 统计检验）
- [ ] 如用 IV：第一阶段 F>10，排除限制有论证
- [ ] 控制变量选择有依据（非 kitchen-sink）
- [ ] 固定效应层级合理
- [ ] 标准误聚类层级说明

产出：`04-数据/变量定义.md`（变量名、定义、来源、描述统计预期）

### 阶段 4：数据构建

**定位**：do 文件组织 + 数据质量检查。使用现有 Stata do 文件体系。

> **Stata 编码规范**：进入本阶段时，先加载 `stata-regression` 技能获取 do 文件模板、编码规范、输出标准。写 do 文件前查路由表，打开对应 reference 文件，不要凭记忆拼命令。

#### Python 预处理（可选，视数据源类型而定）

部分数据任务用 Python（research env）比 Stata 更高效，尤其是：

| 任务 | 适用场景 | Python 方案 |
|------|---------|------------|
| **多源数据 merge** | CSMAR 各表格式不一致（Stkcd str6/long/double） | pandas merge + astype 统一类型 |
| **Excel 原始数据清洗** | 手工收集/统计局的表格数据 | pandas + openpyxl |
| **PDF 表格提取** | 政策文件/统计年鉴中的结构化数据 | 配合 MinerU 产出做后处理 |
| **API 数据拉取** | 从天眼查/企查查/政府开放平台获取 | requests + pandas |
| **文件格式转换** | CSV → dta 或 dta 转 parquet | pandas to_stata / pyreadstat |

调用方式：
```bash
# Hermes 中执行
terminal("pyresearch scripts/merge_csmar.py", workdir="<项目工作文件夹>")
# 或直接
pyresearch -c "import pandas as pd; df = pd.read_stata(...); ..."
```

产出写入 `04-数据/数据构造/` 下的子文件夹，与 Stata 的 dta 文件同目录。

#### Stata 主流程

流程：
1. 原始数据导入（CSMAR/Wind/手工）
2. 数据清洗（缺失值、异常值、winsorize）
3. 变量构造（gen/egen/merge）
4. 样本筛选（行业/年份/ST/缺失）
5. Table 1 描述统计

检查清单：
- [ ] winsorize 阈值有说明（1%? 5%?）
- [ ] 缺失值模式已检查（misstable patterns）
- [ ] merge 结果已验证（_merge==3 比例）
- [ ] 平衡性检验（treated vs control 在处理前可比）
- [ ] 样本量变化每步记录（为什么从 N1 变成 N2）

产出：do 文件 + `04-数据/数据构建记录.md`

### 阶段 5：实证分析

**定位**：按标准经济学论文顺序执行。使用现有 Stata do 文件体系。

> **Stata 技术底层**：进入本阶段时先加载 `stata-regression` 技能。回归模板和出图模板从该技能的 reference 文件获取，计量检查清单用于执行后验证。本阶段负责「用正确的方法跑回归」，`stata-regression` 负责「回归应该怎么写才规范」。

**分析命令文件夹结构（强制执行）**：

所有回归命令必须集中在一个文件夹内，本地和 Obsidian 双份同步：

```
本地: <项目工作文件夹>/数据/working data/分析命令/
Obsidian: <vault>/研究/<项目名>/论文写作/04-数据/分析命令/
```

> 在 Obsidian 目录下放置 README.md（含文件清单和运行方式），do 文件本体放本地。log 和 CSV 输出自动生成在本地 `分析命令/`。**Do 文件编码规范（header/version/logging/esttab 选项）见 `stata-regression/references/do-file-standards.md`。**

**执行顺序**（严格按此顺序，不可调换）：

1. 描述统计 + 相关性分析（最前面；Table 1 含企业+省级全部控制变量，三位小数）
2. 基准回归 V1（post，三列逐步：post only / +企业控制 / +省级控制，9列全表）
3. 基准回归 V2（post，同上，9列全表）
4. **处理强度：连续得分（全样本）**——用 treat_score1（0-5）在全样本上回归，6列（dev/over/under × V1×2），直接跟在主回归后作为第三张主回归表。不要用二元组间比较（d_zhengting/d_admin_gov），原因是限定子样本会导致聚类数骤降（31→15-20省）且控制组定义模糊。
5. 平行趋势检验 V1+V2（事件研究法，强制）
6. 稳健性检验 V1+V2（竞争性政策、政府词频、PSM、Leave-One-Out）
7. 内生性处理 V1+V2（IV，控制变量须与主回归一致）
8. **机制检验 V1+V2**（三步法）

   > ⚠️ **机制检验必须先读 `references/mechanism-exploration-protocol.md`**，按探索协议从 Step A 到 Step E 顺序执行。**禁止跳过 Step A/B 直接跑交互项**——没有直接效应基础就上交互项等于盲试。
   >
   > 三条硬约束：
   > 1. 固定特征 M（pc_any/SOE）→ 不可用三步法，直接降级为交互项或废弃；
   > 2. 连续变量优于二元分组（RDIN > suspect_rd）；
   > 3. 每条死路必须在理论推演或仪表盘中记录。
9. 异质性分析 V1+V2（产权/市场化/SRDI）
10. 进一步检验（2026-08 决策：经济后果检验**不做**）——进一步分析内容 = 方向性延伸（under/over 描述性收敛 + TWFE 负权重诊断）+ 处理强度 treat_score1。⚠️ 经济后果承诺须在文献综述中删除（2026-08 教训：综述承诺了未做的检验）

**实证表格格式规范**：表格输出标准（小数位、括号内 t 值、esttab 选项）→ **加载 `stata-regression` 技能，查 `references/table-standards.md`**。

**Stata 技术坑**：完整的 Stata 编码规范、出图标准、表格格式、计量检查清单、24 条陷阱速查 → **加载 `stata-regression` 技能**。本阶段仅保留论文层面的坑（CSMAR 合并细节、V1/V2 策略等）。

**机制检验三步法**（参考：江艇 2022，中国工业经济）：

> 2026-08 P1-3 定案：正文报告 Step 2（机制变量作 DV 的 DID）+ 异质性佐证；Step 3（M 进主回归）移至附录，表述为「与机制一致的相关性证据」。方法依据仍为江艇(2022)。论文中称「机制检验」，不称「中介检验」，不使用"中介效应"这一术语。
>
> 文件路径: `~/Zotero/storage/7MWPPZ3U/因果推断经验研究中的中介效应与调节效应(1).pdf`

- Step 1: X → Y（主回归已做，确认主效应存在且显著）
- Step 2: X → M（正文报告：机制变量作 DV 的 DID + 异质性佐证）
- Step 3: X + M → Y（移至附录，表述为「与机制一致的相关性证据」）
- 报告格式：正文报告 Step2 列（M）；Step3 相关表移入附录

**实证表格格式规范（强制执行）**：

> 🚨 以下规则替代「列个摘要就行」的思维方式。实证部分的产出是**可以直接插入论文的表格**，不是草稿。

- [ ] **不省略控制变量**：所有回归表格必须列出全部控制变量的系数和标准误，不得只报告 post 系数
- [ ] **固定效应标注**：每列表格下方标注是否包含企业 FE、年份 FE、省份聚类
- [ ] **样本量 + Adj R²**：每列必须报告 N 和 Adjusted R²
- [ ] **主回归三列制**：每个 DV 至少跑三列——(1) post only，(2) +企业控制变量，(3) +省级控制变量
- [ ] **V1/V2 分表**：如果 DV 有多个版本（如 V1/V2），分成两张独立表格，不要拼成超宽表
- [ ] **描述统计含省级变量**：Table 1 必须包含企业+省级全部控制变量，保留三位小数
- [ ] **相关性分析必须出现**：放在描述统计之后、主回归之前
- [ ] **稳健性检验为独立表**：竞争性政策、政府词频、PSM、安慰剂检验各自独立成表
- [ ] **Leave-One-Out 逐省剔除**：逐省剔除后检验 post 系数是否仍显著，报告系数范围

**检查清单**：

- [ ] **描述统计含全部控制变量**：企业+省级全部变量，三位小数。不允许只列核心变量
- [ ] **相关性分析必须出现**：描述统计后、主回归前
- [ ] **绝对量变量对数化**：fiscal_ratio、GDP、wage 等值域过大变量须 `gen ln_x = ln(x)`
- [ ] **DID 平行趋势检验**：事件研究法。**Stata 实现见 `stata-regression/references/graph-templates.md`**。
- [ ] bacondecomp（staggered DID 时）
- [ ] 安慰剂检验（随机置换处理组）
- [ ] 弱 IV 检验（如用 IV）
- [ ] 过度识别检验（如多 IV）

> 📋 完整表格格式规范见 `references/empirical-table-format-spec.md`。**Stata 编码规范、出图标准、表格格式、计量检查清单** → 加载 `stata-regression` 技能。CSMAR 数据合并陷阱速查见 `references/csmar-data-pitfalls.md`。机制检验方法论见 `references/mechanism-testing-methodology.md`。**机制探索协议见 `references/mechanism-exploration-protocol.md`**（探索顺序、固定特征检测、死路记录）。**PDF表格提取方法见 `references/pdf-extraction-workflow.md`**。**省级大数据局数据来源见 `references/data-sourcing-province-bureaus.md`**。

产出：
- 分析命令/master_analysis.do + log 文件 + CSV 表格
- `05-实证/主回归结果.md`
- `05-实证/稳健性检验.md`
- `05-实证/异质性分析.md`
- `05-实证/机制检验.md`

### 5.1 Stata 执行规范（硬约束）

**在跑任何 Stata 命令之前——reg、机制、出表出图——必须先执行：**

```
skill_view(name='stata-regression')
```

完成后按以下步骤执行：

1. **路由**：从 Routing Table 确认任务类型（写 do/出图/出表/检查）
2. **读模板**：打开对应 reference 文件
3. **检查**：对照 `econometric-checklist.md` 确认参数
4. **运行**：Stata do 文件
5. **输出**：`esttab2html.py` 处理 .tex → .html + .docx
6. **验证**：检查 log 无报错 + 输出路径正确

> 这条规则的触发条件是「任何涉及 Stata 执行的消息」——不等用户说是加载。

**实证问题溯源**：当运行结果出现意外模式（平行趋势失败、系数方向与预期相反、关键结果不显著等）时，**在执行回退或调整之前**，先加载 `research-discovery` 技能按六步流程处理（冻结结果 → 分层诊断 → 差距分析 → 机制探索 → 决策路由 → 沉淀），诊断定位到方法层、需要外部实操方案时再加载 `research-media-skill` 搜索经管之家论坛，避免重复试错。发现过程和结论写入 `研究发现/01-发现/F-xxx.md`，检索结论和采纳方案记入仪表盘迭代记录。

### 5.2 实证修改后的 Obsidian 同步（强制执行）

**每次 do 文件或实证结论发生任何变化后，必须立即按以下顺序同步三处：**

1. **`05-实证/主回归结果.md`**：表格增删、列重编号、系数更新。保留全部详细系数行，不缩为摘要。
2. **`04-数据/分析命令/README.md`**：更新表号和文件清单。
3. **`00-项目仪表盘.md`**：
   - 更新「实证结果摘要」表中涉及变更的条目；
   - 在「关键决策记录」中追加一条（如「V2 移至稳健性」「删除 d_zhengting 二元比较」）；
   - 在「迭代记录」中追加一条，格式：`### 迭代 N（日期）: 变更简述 → 结果`；
   - 在「会话记录」中追加本次摘要。

**同步检查清单（改完 do 文件后逐条确认）：**
- [ ] do 文件已 cp 到本地工作文件夹？
- [ ] `05-实证/主回归结果.md` 表格编号和列数已更新？
- [ ] `04-数据/分析命令/README.md` 表清单已更新？
- [ ] `00-项目仪表盘.md` 状态、决策、迭代记录已追加？
- [ ] `01-选题/研究计划.md` 如含过时信息（如已废弃的 V3/d_zhengting）已更新？
- [ ] `03-理论/理论推演.md` 如假设受实证结果影响已更新？

**常见遗漏模式**：
- 删了 d_zhengting 但研究计划/理论推演里还在引用 → 搜索 `d_zhengting`、`V2`、`V3` 等关键词定位残留
- 新增了 treat_score1 但理论推演里没有对应假设 → 补充 H1d
- V2 移入稳健性但研究计划仍写「主回归 V1+V2 双轨」

> **Stata 编码规范、出图标准、表格格式、计量检查清单** → 加载 `stata-regression` 技能。机制检验方法论见 `references/mechanism-testing-methodology.md`。实证探索策略与常见死路见 `references/empirical-exploration-patterns.md`（机制检验顺序、处理强度连续得分 vs 二元比较、研发操纵连续指标）。

### 5.3 实证版本管理协议（强制执行，2026-08-07 定案）

**背景**：2026-08-07 全稿一致性审查暴露新旧版本混杂的系统性风险（正文沿用旧样本数字、CSV 与表号脱节、无出处数字、do/log 版本错位）。本协议在每轮实证修改中强制执行。

**核心概念：Run Tag**——每次全量重跑或样本口径变更 = 一个运行标记 `YYYYMMDD_vN`（如 `20260807_v3`）。所有产物（CSV 目录、do 归档、log、正文状态行）绑定同一 tag。

1. **CSV 命名 = 正文表号**：输出至 `output/tables/rerun_<tag>/table<NN>.csv`（NN 为正文表号，如 table04 = 正文表 4）。正文表号重排时同步重命名 CSV；禁止靠"内容匹配"猜对应。
2. **四件套绑定**：一张表 = do 段落 + log + CSV + 正文表格 + 状态行标注，五者缺一即该表数字不可追溯（按 P0 处理）。正文引用表格前先确认四件套齐全。
3. **改数字五步闭环**：改 do → 重跑（log 更新）→ CSV 更新 → 正文表格更新 → 状态行更新。禁止只改正文不重跑、只重跑不改 CSV。do 与 log 的样本口径必须一致（如 keep 条件），不一致视为版本错位。
4. **版本切换协议**：换样本/全量重跑 = 新 tag + 新输出目录，旧目录移入 `archive/rerun_<旧tag>/`，禁止原地覆盖。切换完成后执行**旧值扫描**：grep 旧样本特征值（样本量、关键系数、样本期）确认正文零残留，再更新 docs（数据构造.md/变量说明.md 的样本期与变量清单）。
5. **数字出处纪律**：正文每个数字必须可回溯到 log 或 CSV（记行号）；"历史备忘"类过程文档（如 主回归结果.md 的决策记录）不产出表格数字；无出处数字写入正文 = P0，须重跑补出处。
6. **临时 do 归档**：/tmp 调试 do 跑通后必须归档至 `dofiles/<tag>/`，文件头注明 tag、用途、产出表号；分析命令 README.md 同步登记。
7. **log 强制留存**：每次运行 log 命名 `logs/<tag>/<表号>_<内容>.log`；0 字节 log = 未跑成功 = 该表数字无效，须重跑。
8. **每 run 一份 MAPPING.md**：run 目录内放表号 ↔ CSV ↔ do 段落 ↔ log ↔ 样本口径对照表。同一结果多份日志（不同样本口径）必须标注口径，禁止未标注并存。
9. **正文状态行 = 版本声明**：每章头部状态行写 `> 版本：<tag>（<样本期>）`；任何数字修改后立即更新状态行（含"待补跑"标记的清除）。
10. **一致性审计**：每次会话结束或阶段 5 结束时，跑一次一致性扫描：正文特征值 grep（旧样本量/旧系数/旧样本期）+ 抽查 1-2 张表回读 CSV 逐格核对。审计结果记录到仪表盘迭代记录。

**常见违规模式**（发现即修）：
- 正文用旧样本数字：旧样本量（如 25,300/26,800）或旧系数与 rerun 目录 CSV 不符
- CSV 文件名与表号脱节：正文表 6 的 CSV 叫 full_table3.csv
- 数字只有"历史备忘"出处：log/CSV 中 grep 不到
- do 改了没重跑：do 与 log 的 keep 条件/样本期不一致

---

## 实证设计惯例（阶段 5 补充，2026-07）

以下为本会话验证过的设计决策，后续论文项目可直接套用。

### V1/V2 的主次关系

DV 有多个度量版本时，**V1 做主回归，V2 做稳健性**。不要 V1+V2 并排两张主回归表。理由：
- CSSCI 主回归 1-2 张表，V1+V2 18 列过于臃肿；
- 「替换 DV 度量后结果不变」是稳健性经典范式，比并列展示更有说服力；
- 全文异质性、平行趋势、IV 也只跑 V1，V2 仅出现在稳健性一节。

### 处理强度：连续得分 > 二元分组

处理变量有天然强度梯度（如大数据局级别×性质 0-5），**用连续得分全样本回归**，不限定 treated 子样本做二元比较。理由：
- 限定子样本后聚类数从 31 降至 15-20，Stata 报警「missing F statistic」；
- 二元分组控制组定义模糊（副厅 vs 正处/挂牌？行政 vs 事业 vs 部门管理？）；
- 连续得分直接放入主回归，作为剂量-反应证据，表格 3-6 列。

### 经济后果与机制的探索顺序

不要一上来就套 `dev×post` 交互项。从简到繁：
1. **直接回归**：`outcome ~ post` 或 `outcome ~ dev`（无交互）
2. **分组**：按 suspect_rd/SOE 等做子样本回归看符号差异
3. **交互项**：`outcome ~ dev×post` 仅在步骤 1-2 有信号时尝试

研发操纵类指标优先用连续变量（RDIN）而非虚拟变量（suspect_rd）。

### 机制检验批量策略（2026-07 验证）

**10+ 条机制的教训：绝大多数 M 的 post→M 就不显著。** 不要逐条跑、逐条等结果。策略：

1. **批量构造 → 批量跑 Step 2**：一次把 5-8 个候选 M 变量全构造好，一个 do 文件全部跑完，只输出 `M ~ post` 的系数和 p 值
2. **p<0.05 的才进入 Step 3**，其余直接废弃，节省轮次
3. **连续变量 > 二元分组**：RDIN > suspect_rd，研发投入强度 > 研发人员占比，分析师跟踪数 > 非标审计意见
4. **已验证可行的一条**：`RDSpendSumRatio ~ post`（研发投入强度，p=0.046，方向正）——来自 CSMAR 研发投入表 `PT_LCRDSPENDING.dta`
5. **同源数据统一 merge**：避免重复加载大 CSMAR 文件
6. **Stkcd 类型注意**：CSMAR 各表 Stkcd 格式不一致（str6 / long / double），merge 前务必检查并统一类型

### 常见死路（直接跳过，不要重试）

---

## 阶段 5→6 准入审查（强制执行）

在进入阶段 6 之前，必须完成以下三项检查。仪表盘标记为「完成」不等于真的完成——文件夹可能不存在，内容可能已过时。

### 审查 1：文件夹存在性

检查 Obsidian vault 中是否存在（均在 `研究/<项目名>/` 下）：
- `论文写作/03-理论/`（至少含制度背景.md）
- `论文写作/05-实证/`（至少含主回归结果.md）
- `论文写作/06-论文稿/`（可为空，写作阶段填充）
- `研究发现/`（位于项目根，与 论文写作/ 平级；如实证过程中出现过意外结果，`研究发现/01-发现/` 必须存在 F-xxx 发现文件）

缺失 → 立即补建，补写缺失内容，才能继续。

### 审查 2：文献综述与实证设计一致性（⚠️ 关键）

**读取 `02-文献/文献综述.md`（或最新版本），逐条核对：**

- [ ] 综述中描述的被解释变量（DV）与实际 working data 中的 DV 一致？
- [ ] 综述中描述的分析层级（企业层面 / 省-行业-年层面 / 省-年层面）与实际一致？
- [ ] 综述中描述的识别策略（DID/IV/其他）与实际一致？
- [ ] 综述中的「本文贡献」段落与实证发现对齐？

**常见脱节模式**：
- 综述写的是省份-行业-年层面的「补贴-研发敏感度」，但实际数据跑的是企业层面 Richardson 残差法 → **必须重写综述**
- 综述引用了 V3 词频法 DV，但 V3 已被实证排除 → 删除引用

不一致 → 标记为「准入失败」，**先修文献综述再进入写作**。不要用「边写边改」的侥幸策略——引言会跟着综述跑偏。

> 真实案例：`references/lit-review-empirical-mismatch.md`——综述写的是省份-行业-年敏感度，数据跑的是企业残差，整篇综述需要重写。

### 审查 3：实证素材就绪

检查 `05-实证/` 下至少包含：
- 主回归结果（完整系数表）
- 稳健性检验 log 摘要
- 异质性分析结果
- 机制检验结果（如有）

缺失 → 从 log 文件提取。

三项审查全部通过后，才能进入阶段 6 写作。

---

## 阶段 6：论文写作

**定位**：章节生成 + meng-skills v2 润色。

### 6.0 实证表格写入 Obsidian（先于文字撰写）

在动笔写正文之前，必须先把所有实证表格从 CSV 转为 markdown 写入 `05-实证/主回归结果.md`：

1. 确认 `分析命令/` 下所有 CSV 已生成且 log 正常关闭
2. 运行 `esttab2pipe.py`（位于 `stata-regression/scripts/esttab2pipe.py`）将 CSV 转为 markdown pipe table
3. 写入 `05-实证/主回归结果.md`，每张表含完整系数+标准误+表注
4. **绝不省略控制变量**：表必须有 N 列控制变量系数行，不可用 ✓ 缩写
5. 同步 do 文件到 Obsidian `04-数据/分析命令/`，并同步到本地工作文件夹

> ⚠️ **Obsidian 文档重构规则**：修改 `05-实证/主回归结果.md` 时，**保留全部详细系数表，仅删除被移除的列/行**。禁止将 13 张详细系数表缩为摘要段落——用户需要的是可以直接复制到论文草稿的完整系数，不是节约版面的概述。典型错误：删除 V2 列后把整张表切成「完整系数见CSV」。正确做法：删掉 V2 所在列的所有数据行，V1 的每个系数和 t 值一个不漏。重写前务必确认新文件的总行数不少于旧文件（除非删了大量列）。

> ⚠️ **do 文件修改铁律**：**Do 文件编码规范（version/header/esttab/logging/禁止模式）→ 加载 `stata-regression/references/do-file-standards.md`。** 以下仅保留论文层面的规则：
> 1. 先验证再写入：对 do 文件做任何改动前，先写临时 do 文件跑一遍检验结果。用户确认结果可行后，再用 patch 写入正式文件。
> 2. 改 do 必改 Obsidian：每次修改 do 文件后，必须同步更新 Obsidian 中的 `05-实证/主回归结果.md`（新增/删除/重编号表格）和 `04-数据/分析命令/README.md`（文件索引），并 cp 到本地工作文件夹。三处不一致会导致跨会话混乱。

### 6.1-6.4 写作顺序
1. **研究设计 + 实证结果**（先写最硬的部分）
2. **理论分析与研究假设**（基于实证结果反推确认）
3. **引言**（最后写，知道全貌后才能精确定位贡献）
4. **结论与政策建议**

每部分流程：
1. 从 Obsidian 对应文件夹读取素材
2. 按模板生成初稿
3. 调用 meng-skills v2 润色（去 AI 味 + 学术规范）
4. 存入 `06-论文稿/`

检查清单：
- [ ] 摘要按"问题—样本—方法—发现—机制—贡献"推进
- [ ] 贡献表述务实，避免"具有重要理论意义"
- [ ] 结果报告准确（表号、列号、系数、显著性、经济含义）
- [ ] meng-skills v2 自动化验证通过（半角引号/AI词汇/否定句式/破折号）

### 6.5 正文文件组织规范（2026-08 定案）

正文分节写入 `06-论文稿/正文/`，分节文件：

- `00-标题摘要.md`
- `01-引言.md`（含文献综述内嵌）
- `02-制度背景.md`
- `03-理论假设.md`
- `04-研究设计.md`
- `05-实证结果.md`
- `06-机制分析.md`
- `07-异质性.md`
- `08-进一步分析.md`
- `09-结论.md`
- `附录.md`
- `参考文献.md`

每节写作流程：从 Obsidian 对应文件夹读素材（`主回归结果.md` / `理论推演.md` / `制度背景.md`）→ 生成初稿 → meng-skills v2 润色 → 存入分节文件。

写作顺序沿用 6.1-6.4：研究设计+实证 → 理论 → 引言 → 结论。

---

### 6.6 参考文献数量与结构规范（2026-08-10 定案）

**数据来源**：Zotero 库 7 本 CSSCI 期刊 15 篇论文抽样统计（管理世界/经济研究/金融研究/会计研究/中国工业经济/管理科学学报/南开管理评论）。

**总量基准**：
- 目标期刊实际：29–92 条，中位数 42、均值 52，**普遍 35–55 条，鲜少低于 30**
- CSSCI 投稿稿建议：**40–55 条，不低于 35 条**。低于 30 条 = 明显单薄，审稿人第一眼即扣分

**中英文占比基准**：
- 目标期刊中文占比：25%–87%，均值约 58%
- 经济学刊（经济研究/中国工业经济）：中文为主，65%–87%
- 管理学刊（南开管理评论/管理科学学报）：英文比重更高，可达 50%–75%
- 公司金融方向 CSSCI 投稿稿建议：**中文占比 ≥50%**（英文文献是国际方法/理论经典，中文文献覆盖国内制度与实证证据，缺一不可）

**自查动作（参考文献表定稿前）**：
1. 数总数：低于 35 条 → 按主题补引（国内制度背景、识别方法文献、机制相关实证）
2. 算中英文占比：中文 <50% → 优先补中文文献（知网检索同主题国内研究）
3. 逐条核对：作者/年份/期刊/卷期/页码（CNKI 浏览器通道 + Crossref，见 chinese-literature 技能）

---

## 阶段 7：打磨与投稿

**定位**：5 阶段打磨流水线 + 期刊匹配。

### 7.1 打磨流水线

借鉴 AERS paper-pipeline 的 5 阶段顺序（不可调换）：

| 步骤 | 操作 | 工具 |
|------|------|------|
| 1. 机械润色 | 修正格式、标点、引用格式、图表编号一致性 | meng-skills v2 |
| 2. 自我审稿 | 方法论检查（识别假设、稳健性完整性、论述逻辑） | AERS identification audit 方法论 |
| 3. 结构调整 | 标题精炼、摘要重写、章节过渡优化 | meng-skills v2 |
| 4. 二次润色 | 修正第 2-3 步引入的新问题（交叉引用、段落衔接） | meng-skills v2 |
| 5. 引用校验 | 每条引用都可追溯到原文（DOI/页码） | Zotero + citation-checker |

### 7.2 期刊匹配

- 按目标 CSSCI 层次推荐候选期刊
- 检查格式要求（字数/章节/引用格式）
- 生成投稿清单

> 审稿人质疑实证结果时：加载 `research-discovery` 技能重新审视（触发条件含"审稿质疑"），按六步流程诊断，结论记入 `研究发现/`。

产出：`07-投稿/终稿.md` + `07-投稿/打磨记录.md`

### 7.3 输出转换（2026-08 定案）

1. **分节预览**：每节完成后用 pandoc 转 PDF 快速检查排版
2. **终稿转换**：全部完成后用 `chinese-docx-generation` 技能生成投稿版 docx（CSSCI 格式：标题黑体 / 正文宋体五号 / 三线表 / 参考文献 GB/T 7714）
3. **源稿保留**：`07-投稿/终稿.md` 保留为源稿，docx 为投稿版本

### 7.4 审稿意见处置流程（2026-08 定案，第三方模型审稿验证）

**第一步：审稿外包（建议，非强制）**

投稿前可将全文交由**其他模型**（如 Kimi K3、Claude）独立审查。单一模型存在系统性盲区（训练语料、方法偏好、误判模式），外部模型交叉审查可暴露自审盲点；实操中外部模型亦有误判（简化推理挑战 Stata 实际输出、读错表格结构），故**外包审查的意见仍需逐条核实，不可照单全收**。工具：`cli-agent-delegation` 技能（K3 调用 `~/.kimi-code/bin/kimi -m kimi-code/k3 -p`，任务书 = 全文状态行剥离 + 前轮意见 + 本轮修改说明，输出按 P0/P1/P2 分级 + 通过项确认 + 拒稿理由）。

**第二步：意见分级**

- **P0**：数据/数字/表格硬伤（正文与 CSV 不符、表格破损、断链）——必须立即修
- **P1**：必改（方法表述、论证逻辑、引用错误、样本量口径）——逐条处置
- **P2**：可选（格式、建议性引用、锦上添花）——按用户逐条拍板

**第三步：逐条核实真伪（铁律）**

每条意见先核实再处置，特别是涉及数字的指控：
- 系数/SE/星号 → 从 log 或 CSV 重跑验证（esttab 星号来自实际 p 值，勿用"聚类数 t 临界值"简化推理反驳/接受）
- 表格结构 → 直接读正文表格与 CSV 对照
- 引用 → CNKI/Crossref 查证（chinese-literature 技能）
- 核实方法：写临时 do 重跑，或 read_file 读 CSV 逐格对照
- **驳回需给出证据**（如"重跑验证实际 p 值与星号一致，指控不成立"），接受需给出修改

**第四步：处置决策**

| 判定 | 动作 |
|------|------|
| 属实 | 直接修（数字修改走五步闭环：改 do → 重跑 → CSV → 正文 → 状态行） |
| 误判 | 驳回，附重跑/核对证据 |
| 涉及叙事/结构 | 列方案给用户拍板（如"替换 DV 构造升格主结果 vs 维持稳健性"） |

**第五步：复核闭环**

全部处置完成后：
- grep 旧值零残留（旧系数/旧样本量/旧表述关键词）
- 抽查 2-3 处修改点读回文件确认
- 状态行 + 主回归结果.md + 写作备忘同步更新
- 重大处置记录到研究发现/（如"重跑暴露分组口径错误"）

**第六步：响应信（response letter）**

逐条回复格式：`意见原文摘要 → 处置（修改/驳回+证据/部分采纳）→ 修改位置（章节/表号）→ 修改后数字`。驳回项必须给出可验证的证据链（log 行号/CSV 单元格/重跑输出），不写"感谢指出"式空话。

**教训沉淀（2026-08-10）**：
- 外部模型误判实例：以"聚类数 t 临界值"简化推理质疑显著性星号——重跑验证实际 p 值与星号一致，驳回
- 重跑暴露真实问题实例：为满足审稿人对置换次数分辨率的要求重跑，暴露分组变量口径错误（常数边界 vs 样本分位数边界）与论证逻辑缺陷（原论证方向与机制预测相反），经验 p 值反转、显著性结论翻转，正文论证整体改写——**重跑是审稿处置的照妖镜**
- 批量文本替换会伤范围外内容：去卷号正则作用域 bug 误删英文节卷号，全文通读才暴露——**批量替换前先圈定作用域，改完全文复查**

---

## 仪表盘更新协议

**每次会话结束时**，更新仪表盘的以下字段：

1. `last_session`: 更新为当前日期
2. 当前阶段的子任务状态
3. 新增产出文件路径
4. 如触发了决策门：记录决策结果和下一步
5. `会话记录`: 追加一条摘要

格式：
```markdown
- [2026-XX-XX] 阶段 X 工作内容摘要。产出：文件A、文件B。决策：继续/回退。
```

## 引用规范

本 skill 的方法论框架借鉴了：
- AERS econfin-workflow-toolkit（67）：中国公司金融实证工作流
- AERS econfin-proposal：Edmans(2024) 质量护栏
- AERS paper-pipeline：5 阶段打磨流水线设计
- AERS Full Empirical · Stata（00.2）：Stata 8 步实证清单

具体方法论内容通过 aers-index skill 按需加载，不重复存储。

## 关键参考文献

- 江艇 (2022). 因果推断经验研究中的中介效应与调节效应. 中国工业经济, (5), 100-120. → 机制检验三步法方法论基础，主张因果推断中用调节效应替代传统中介效应
